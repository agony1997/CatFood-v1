services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      # 移除 SPRING_DATASOURCE_URL=${DATABASE_URL}
      # Spring Boot 会自动检测并使用 Render 提供的 DATABASE_URL 环境变量
      - SPRING_PROFILES_ACTIVE=prod
    restart: unless-stopped
